"use client";

export default function SearchPreview({
  currentTerm,
  setData,
}: {
  currentTerm: string;
  setData: (term: string) => void;
}) {
  const var1 = currentTerm;
  const var2 = setData;
  return "";
}
